-- config.lua
-- Contains all the global constants for the game.

local Config = {
    SQUARE_SIZE = 20,
    MOVE_STEP = 20,
    SLIDE_SPEED = 200,
    FLASH_DURATION = 0.2,
    ATTACK_COOLDOWN_GLOBAL = 0.1,
    BASE_CRIT_CHANCE = 0.05,
}

return Config